// components/UI/MobileStyles.jsx
import React from 'react';

/**
 * MobileStyles - NO-OP (STAGE stretch-fit)
 *
 * L'architecture actuelle utilise un scale(x,y) global du STAGE (comme v1).
 * Toute logique @media / vw/vh/clamp ici casserait la synchro UI/world.
 */
export default function MobileStyles() {
  return null;
}
